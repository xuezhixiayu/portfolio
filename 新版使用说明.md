# 郑佳辉 · 三套作品集设计

双击 `start-preview.cmd`，然后打开 http://127.0.0.1:4173/ 。需要本机安装 Node.js；本次制作环境已安装。浏览器当前打开旧效果时，刷新页面即可。

## 浏览入口

- 默认作品集： http://127.0.0.1:4173/studio/index.html
- 帧之间： http://127.0.0.1:4173/studio/index.html?theme=frames
- 异世界入口： http://127.0.0.1:4173/studio/index.html?theme=portal
- 会动的漫画： http://127.0.0.1:4173/studio/index.html?theme=comic
- 独立简历： http://127.0.0.1:4173/studio/resume.html
- 后台： http://127.0.0.1:4173/studio/admin.html

三个设计名称只用于风格选择。首页默认展示姓名，主标题可单独修改。作品、经历、关于使用同页滚动导航；点击作品在当前页面展开，原平台视频保留原平台观看入口。

## 自定义

首次预览管理密码：`admin123`。在「管理密码」中修改。

进入「外观与特效」，选择要编辑的风格：

1. 修改首页主标题、开场短句、标题字体和字号。
2. 选择首页主作品。漫画的中央大格、悬浮画面的主画面、异世界的整屏开场分别使用此设置。
3. 使用「首页作品顺序」的上移、下移按钮排序。每套设计分别保存顺序；其余开场画面和作品区采用此顺序。
4. 更换强调色、深浅底色、开场图片或静音循环视频，调整封面取景。
5. 调整全站动效强度，开启或关闭鼠标视差与胶片颗粒。
6. 点击「保存并应用」，再点「查看已保存效果」。选择并保存的风格也成为网站默认风格。

标题字体使用本机字体，无需下载外部字体文件。三个选项为现代黑体、粗体海报、宋体/衬线；不同设备的实际字体可能略有差异。字号作用于首页主标题，简历保持适合阅读和打印的大小。

作品内容、封面和视频在「作品管理」修改。头像、简介、联系方式、背景音乐在「站点设置」修改。背景音乐需访客主动开启；上传的开场视频静音播放。设备开启“减少动态效果”时，镜头、粒子和自动开场视频停止。

异世界现在是连续的整屏作品场景：向下滚动逐部观看，也可用底部编号或「作品目录」直接定位。电脑上在画面空白处按住鼠标并左右拖动，可拉开裂缝窥见下一部作品；拖动约三成画面宽度，出现「松开，进入下一部」后松手即可切换。距离不足时松手收拢；最后一部继续拖动会回到第一部。静止画面不会保留裂缝。点击「进入作品」在当前页面展开详情。手机保留正常滑动与直接点击。

异世界的「外观与特效」增加了姓名排版位置，以及「每部作品的场景」设置。选择作品后，可以预览并调整上下、左右取景，修改场景色、转场方式及动效强度。默认按作品分类使用电影叠化、墨迹裂缝和空间扭曲；每部作品都可以覆盖默认设置。场景动效设为 0 后使用静态画面和直接切换。开场使用首页主作品，其余场景使用该风格的作品顺序。

图像折射使用浏览器 WebGL；不支持时保留图片、裂缝拖动与 CSS 转场。移动端使用轻量效果。切换标签页、打开作品详情、启用减少动态效果时会暂停后台视频；页面不可见时停止持续动画。

## 保存与导出

2026-09-14 已接入 yywf.top/works 的作品补充：在原有 6 部基础上新增 8 部，重复及已确认的改名作品跳过。刷新作品集或后台时自动补入，保留你的现有修改和外观设置。新作品可在后台编辑、排序和删除，详见「作品导入记录-2026-09-14.md」。

这是本机预览编辑版，编辑数据保存在当前浏览器的 IndexedDB 中，浏览器不支持时使用本地存储。三个风格共用作品信息，各自保存主视觉、字体、颜色与顺序。固定使用同一个地址、浏览器与端口；更换端口、使用隐私模式、清理浏览器数据都会影响本机保存的数据。

带 `?theme=...` 的链接用于指定风格预览；没有该参数的作品集和简历使用后台保存的默认风格。

「导入与导出」可导出完整 JSON 备份、导入原版或新版数据、导出当前默认设计的独立 HTML 首页。JSON 包含自定义图片、视频、字体设置和排序。HTML 包含本地作品图片、音乐、空间背景与当前样式；外部媒体地址和原平台观看仍需联网。独立 HTML 是只读快照，简历和后台保留在完整文件夹中。

后台密码仅用于本机编辑界面，不是服务器账户系统。公开发布时应使用带服务端认证的数据管理；不要把此本机后台当作在线管理服务。

原作品集目录未修改。六部作品的原始标题、描述、链接、标签、封面及原简历文字均保留。新增主题设置在首次保存时写入独立的预览数据，不会覆盖原站点数据。

## 文件

- `studio`：新的作品集、简历与后台页面。
- `shared`：三套设计共用的代码、样式、原始内容和媒体。
- `cinema`、`gallery`：保留的上一轮预览。
- `server.cjs`、`start-preview.cmd`：本地预览入口。

已进行代码语法、内容保留、配置持久化、排序、主题渲染和导出检查，以及滚动定位、裂缝闭合、拖动切换与回弹、目录筛选、媒体暂停和动效降级的模拟运行检查。尚未进行浏览器逐项交互、实际 GPU 着色器及各屏幕尺寸的视觉验收。
